<!DOCTYPE html>
<html>
  <head>
    <title>My番組表</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body data-turbolinks="false" class="bg-white">
    <header>
      <%= render 'layouts/navbar' %>
      <div class="container-fruid">
        <%= render 'layouts/flash_messages' %>
        
        <% if logged_in? && current_page?(root_path) %>
          <div class="bg-primary bg-gradient text-light text-center my-0 py-2">
            <h1>MY番組表</h1>
          </div>
          <div class="text-center text-light border-bottom my-0 pt-2 bg-secondary bg-gradient">
            <h2>自分だけのオリジナル番組表を作ろう！</h2>
          </div>
        <% elsif logged_in? %>
          <div class="bg-primary bg-gradient text-light text-center my-0 py-2">
            <h1>ようこそ<%= current_user.name %>さん</h1>
          </div>
          <div class="text-center text-light border-bottom my-0 pt-3 bg-secondary bg-gradient">
            <p>このページに来たのは・・・<%= l Time.zone.now %></p>
          </div>
        <% else %>
          <div class="bg-primary bg-gradient text-light text-center my-0 py-2">
            <h1>MY番組表</h1>
          </div>
          <div class="text-center text-light border-bottom my-0 pt-2 bg-secondary bg-gradient">
            <h2>自分だけのオリジナル番組表を作ろう！</h2>
          </div>
        <% end %>
      </div>
  </header>

<%= yield %>

  <footer class="bg-secondary bg-gradient text-center m-0 fixed-bottom">
    <div>&emsp;</div>
    <div class="bg-primary bg-gradient">&emsp;</div>
    <div class="text-light bg-dark m-0">created_by&emsp;Cofmada</div>
  </footer>
  </body>
</html>
