<!DOCTYPE html>
<html  lang="ja">
  <head>
    <title>My番組表</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body data-turbolinks="false">
    <header class="m-0 p-0">
      <%= render 'layouts/navbar' %>
      <div class="text-center border-bottom my-0 pb-1 bg-secondary bg-gradient">
        <%= render 'layouts/clock' %>
      </div>
      <div class="container-fruid">
        <%= render 'layouts/flash_messages' %>
        
        <% if logged_in? && current_page?(root_path) %>
          <div class="text-light text-center my-0 py-0">
            <h1 class="m-0">MY番組表</h1>
          </div>
          <div class="wind text-center text-light my-0">
            <h3>自分だけのオリジナル番組表を作ろう！</h3>
          </div>
        <% elsif logged_in? %>
          <div class="text-light text-center my-0">
            <h2 class="m-0">ようこそ<%= current_user.name %>さん</h2>
          </div>
        <% else %>
          <div class="text-light text-center my-0 py-0">
            <h1 class="m-0">MY番組表</h1>
          </div>
          <div class="wind text-center text-light my-0 py-2">
            <h3>自分だけのオリジナル番組表を作ろう！</h3>
          </div>
        <% end %>
      </div>
  </header>

<%= yield %>

  <footer class="bg-secondary bg-gradient text-center m-0 fixed-bottom">
    <div>&emsp;</div>
    <div class="bg-primary bg-gradient">&emsp;</div>
    <div class="text-light bg-dark m-0">created_by&emsp;Cofmada</div>
  </footer>
  </body>
</html>
