<div class="row text-center m-0 px-0">
  <h3 class="col-4 offset-col-8 blue-w py-2">説明</h3>
</div>

<div class="row text-center small m-0 px-0 py-0">
  <div class="writer col-4 wind px-0 py-3">
    <p class="select ev1">MY番組表？</p>
    <p class="select ev2">Ch情報登録</p>
    <p class="select ev3">Ch情報編集</p>
    <p class="select ev4">番組表登録</p>
    <p class="select ev5">番組表一覧</p>
  </div>
  <div class="txt text-start col-8 blue-w m-0 pt-2"></div>
</div>

<% if logged_in? %>
  <div class="blue-w gold text-center pt-2">
    マイページへ<br>
    <%= link_to 'マイページ', user_path(current_user), class: 'metal btn px-2 mb-3 mt-2 py-2' %>
  </div>
<% else %>
  <div class="box blue-w">
    <div class="gold text-center mt-2">
      利用開始<br>
      <%= link_to 'ユーザー登録', signup_path, class: 'metal btn px-2 mb-3 mt-2 py-2' %>
    </div>
    <div class="gold text-center mt-2">
      登録済みの方<br>
      <%= link_to 'ログイン', login_path, class: 'metal btn px-4 mb-3 mt-2 py-2' %>
    </div>
  </div>
<% end %>

<div class="row text-center wind m-0 p-1">
  <div class="col-11 mx-0 px-0">更新情報</div>
  <div class="col-1 acord metal btn p-0">？</div>
</div>
<%= render 'toppages/update' %>
<%= javascript_pack_tag 'user/toppage' %>