<h4 class="blue-w text-center gold pb-2">Ch編集ページ</h4>

<%= render'form',channel: @channel %>

<%= render'video_form', channel: @channel %>

<%= render'video_search', channel: @channel %>

<%= javascript_pack_tag 'user/forms' %>