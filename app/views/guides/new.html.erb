<%= render 'layouts/tabs' %>

<div class="text-center wind m-0 p-1">番組表名入力</div>
<%= render'guideform', guide: @guide, channels: @channels, air_times: @air_times %>

<%= javascript_pack_tag 'user/forms' %>